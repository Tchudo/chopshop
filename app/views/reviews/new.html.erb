<div class="favorite-bg">
  <div class="d-flex justify-content-between p-2">
    <%= link_to '/' do %>
      <i><%= image_tag "lily_35.png" %></i>
    <% end %>
    <h1 class="text-center text-white">Avis</h1>
    <h1> </h1>
  </div>
  <%= link_to stock_path(@stock.id) do%>
  <div class="btn rounded rounded-pill btn-circle bg-warning p-2"><i class="fas fa-chevron-right" style="color: white"></i></div>
  <% end %>

  <div class="container">
    <%= simple_form_for [@stock, @review] do |f| %>
      <%= f.input :comment, label: "Commentaire", error: "Veuillez renseigner ce champ" %>
      <%= f.input :rating, label: "Note", error: "La note doit être comprise entre 0 et 5" %>
      <%= f.button :submit, class: "btn btn-warning", style:"color: white;" %>
    <% end %>
  </div>
</div>

