<div class="container p-0">
  <div class="text-center">
    <div class="img-size">
      <i><%= cl_image_tag stock.product.photo.key%></i>
    </div>
    <div>
      <h6 class="mb-0 mt-1"><%= stock.product.name %></h6>
      <p><%= shop.name %></p>
      <p><%= stock.product.description %></p>
    </div>
    <div class="d-flex justify-content-between align-item-center px-4">
      <h4><%="#{stock.price}€" %></h4>
      <%= link_to stock_path(stock.id) do %>
        <button class="btn btn-warning px-2 py-1">
          <i class="fas fa-chevron-right" style="color: white;"></i>
        </button>
      <% end %>
    </div>
  </div>
</div>
