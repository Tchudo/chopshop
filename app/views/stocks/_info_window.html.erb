<div class="container p-0">
  <div class="text-center">
    <div class="img-size">
      <i><%= cl_image_tag product.photo.key%></i>
    </div>
    <div>
      <h6 class="mb-0 mt-1"><%= product.name %></h6>
      <p><%= shop.name %></p>
      <p><%= product.description %></p>
      <h4><%="#{stock.price}€" %></h4>
    </div>
    <div class="d-flex justify-content-between align-item-center px-4">


    <div class="d-flex justify-content-between align-items-center mb-2">
      <button class="btn p-0 m-0 bg-warning border-O shadow-none" data-toggle="modal" data-target="#ModalNewBasket">
        <i class="fas fa-shopping-basket shadow-none" style="color: white; cursor: auto; font-size: 20px;"></i>
      </button>
    </div>

<!--     <div class="d-flex justify-content-between align-items-center mb-2">
      <button class="btn p-0 m-0 bg-transparent border-O shadow-none" data-toggle="modal" data-target="#ModalNewFavorite"> <i class="fa fa-heart shadow-none"
        <%# if @favorite_user.include? @stock.product.id  %>
        style="color: #f688bb; cursor: auto; font-size: 20px;"
        <%# else %>
        style="color: gray; font-size: 20px;"
        <%# end %>
      ></i></button>
    </div> -->





      <%= link_to stock_path(stock.id) do %>
        <button class="btn btn-warning px-2 py-1">
          <i class="fas fa-chevron-right" style="color: white;"></i>
        </button>
      <% end %>
    </div>
  </div>
</div>


<!-- Modal -->

<!-- ModalNewBasket -->


  <div class="modal fade" id="ModalNewBasket" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg h-75" role="document">
      <div class="modal-content">
        <div class="modal-body">
          <div class="container mt-4 text-center">
            <h5>Voulez-vous ajouter <strong><%= stock.product.name %></strong> au panier ?</h5>
            <div class="mx-5 d-flex justify-content-center">
              <%= simple_form_for [stock, basket] do |f| %>
                <%= f.submit 'Oui', class: 'btn btn-success m-4' %>
              <% end %>
              <button type="button" class="btn btn-warning m-4" data-dismiss="modal">Non</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
